# system monitor
zplug "cjbassi/gotop", as:command, from:gh-r, use:"*linux_arm8.tgz", if:"[[ $(uname -m) == 'aarch64' ]]"
# find alternative
#zplug "sharkdp/fd", as:command, from:gh-r, use:"*aarch64-unknown-linux-musl*", if:"[[ $(uname -m) == 'aarch64' ]]"
# cat alternative
zplug "sharkdp/bat", as:command, from:gh-r, use:"*aarch64*linux-gnu*", if:"[[ $(uname -m) == 'aarch64' ]]"
